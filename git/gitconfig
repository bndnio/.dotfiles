[user]
	name = Brendon Earl
	email = brendonaearl@gmail.com
	signingkey = ~/.ssh/id_ed25519

[gpg]
	format = ssh

[commit]
	gpgsign = true

[gpg "ssh"]
	allowedSignersFile = ~/.ssh/allowed_signers

# Machine- or work-specific overrides (create locally if needed)
[includeIf "gitdir:~/Code/Dopa/**"]
	path = ~/.gitconfig-dopa
